# Currency Converter API - Environment Variables (Secrets Only)
# Copy this file to .env and update with your actual values
# Non-sensitive settings are in appsettings.json

# JWT Secret (REQUIRED - must be at least 32 characters)
JWT_SECRET=DevSecret-minimum-32-characters-long-key-here!!

# Redis Connection (optional, leave empty for in-memory cache)
# Contains password - keep secret
REDIS_CONNECTION=localhost:6379,password=dev-password-change-in-production

# Demo Users (for testing only - remove in production)
# Format: username:password:role (pipe-separated for multiple users)
DEMO_USERS=admin:Admin123!:Admin|user:User123!:User|viewer:Viewer123!:Viewer
