# Production deployment (replace with actual production API URL)
# For Docker Compose: the Dockerfile sets this to "/" to use nginx proxy
VITE_CURRENCY_CONVERTER_API_URL=https://api.yourproductiondomain.com
