# Environment Variables Example
# Copy this file to .env and update with your actual values

# JWT Secret (minimum 32 characters)
JWT_SECRET=DevSecret-minimum-32-characters-long-key-here!!

# Redis Password (used by both Redis container and API)
REDIS_PASSWORD=dev-password-change-in-production

# CORS Configuration
CORS_ALLOWED_ORIGIN=http://localhost:3000

# Cache Configuration (for API)
CACHE_PROVIDER=Redis
CACHE_REDIS_CONNECTION=redis:6379,password=dev-password-change-in-production
CACHE_ABSOLUTE_EXPIRATION_MINUTES=60

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Production
